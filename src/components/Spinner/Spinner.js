const Spinner = () => {
  return (
    <div
      class='spinner-border'
      role='status'
      style={{ width: '60px', height: '60px', color: 'var(--gold)' }}
    ></div>
  );
};

export default Spinner;
